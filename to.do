(noted elsewhere (The Universal-Parser should work with Expect.) (UniLang))

(noted elsewhere
 (the system that does the universal parsing of things, is the
  same as the api-learner, since it has to learn the actions.)
 (api-learner/to.do))

(semantic bovinator has a lot of good parsers)

(can syntax highlight Emacs buffers)

(can look at perl6's parsing/regex replacement system)
